<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your profile</title>
</head>
<body>
    <h1 class="profileTitle">Your Profile</h1>
    <% if(user){ %>
        <h1>You are: <%= user.username %></h1>
     <% } else{ %>
        <h1>Not logged in</h1>
      <% } %>
   

      <% if(user){ %>
        <h1>Your points: <%= user.points %></h1>
     <% } else{ %>
        <p>Log in to see your points!</p>
      <% } %>
</body>
</html>